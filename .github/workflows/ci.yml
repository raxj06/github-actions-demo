name: CI workflow

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout the code
        uses: actions/checkout@v4

      - name: build the docker image
        uses: docker/setup-buildx@v4
